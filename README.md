
## Instalación
```cmd
pip install -r requirements.txt
```

## Entrenamiento
```cmd
python -m train.train
```
(El script usa por defecto `configs/config.yaml` si no se pasa parámetro.)

## Validación embeddings
```cmd

python -m train.validate
```
